[package]
name = "health"
version = "0.1.0"
edition = "2021"

[dependencies]
types = { path = "../types" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

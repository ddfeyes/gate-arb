[package]
name = "strategy"
version.workspace = true
edition.workspace = true

[dependencies]
types = { path = "../types" }
engine = { path = "../engine" }
db = { path = "../db" }
parking_lot = { workspace = true }
tracing = { workspace = true }
